<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:paddingLeft="10dp"
                android:paddingRight="5dp"
                android:paddingTop="5dp"
                android:paddingBottom="5dp">

    <ImageView
            android:id="@+id/recipe_row_imageview"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_launcher"
            android:scaleType="centerCrop"
            android:layout_marginRight="5dp"
            android:contentDescription="@string/image_description"/>

    <TextView
            android:id="@+id/recipe_row_textview_recipe_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_toRightOf="@id/recipe_row_imageview"
            android:maxLines="1"
            android:ellipsize="end"/>

    <TextView
            android:id="@+id/recipe_row_textview_recipe_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_toRightOf="@id/recipe_row_imageview"
            android:layout_below="@id/recipe_row_textview_recipe_name"
            android:maxLines="2"
            android:textSize="12sp"
            android:ellipsize="end"/>

</RelativeLayout>