<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="de.anycook.app">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:icon="@drawable/anycook"
            android:logo="@drawable/anycook_transparent"
            android:theme="@style/AnycookTheme">

        <activity android:name=".activities.MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--<activity android:name=".activities.GroceryListActivity" android:label="@string/app_name">
            <meta-data android:name="android.app.default_searchable"
                       android:value=".activities.RecipeAutoCompleteActivity"/>
        </activity>
        <activity android:name=".activities.RecipeAutoCompleteActivity"
                  android:launchMode="singleTop"
                  android:parentActivityName=".activities.GroceryListActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data
                    android:name="android.app.searchable"
                    android:resource="@xml/searchable"/>
        </activity>
        <activity android:name=".activities.AddIngredientsActivity"
                  android:parentActivityName=".activities.RecipeAutoCompleteActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="de.anycook.app.activities.RecipeAutoCompleteActivity"/>
        </activity>
        <activity android:name=".activities.LocationActivity"
                  android:parentActivityName=".activities.GroceryListActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="de.anycook.app.activities.EditGroceryListActivity"/>
        </activity> -->
    </application>

</manifest>
